<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ufind.shafique.alc40">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label= "ALC 4 Phase 1"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".About_Activity"
            android:screenOrientation="portrait"
            android:icon="@drawable/andela"
            android:description="@string/abt_alc"
            android:label="@string/about_alc"/>

        <activity android:name=".Profile_Activity"
            android:screenOrientation="portrait"
            android:description="@string/profile"
            android:label="@string/profile"
            android:icon="@drawable/andela"/>


    </application>

</manifest>